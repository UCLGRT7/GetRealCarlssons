
<Window x:Class="GetRealCarlssons.Views.CreateProjectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Opret Projekt" Height="600" Width="800">
    <ScrollViewer>
        <StackPanel Margin="20">
            <TextBlock Text="Navn:" />
            <ComboBox ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" />

            <TextBlock Text="Sagsnummer:" />
            <TextBox Text="{Binding CaseNumber}" />

            <TextBlock Text="Projekt Addresse:" />
            <TextBox Text="{Binding Address}" />

            <TextBlock Text="Deadline:" />
            <TextBox Text="{Binding Deadline}" />

            <TextBlock Text="Omfang (1-10):" />
            <TextBox Text="{Binding Scope}" />

            <TextBlock Text="Ydelser:" Margin="0,10,0,0"/>
            <ItemsControl ItemsSource="{Binding Services}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBox Width="200" Text="{Binding Name, Mode=TwoWay}" />
                            <TextBox Width="50" Margin="10,0,0,0" Text="{Binding Complexity, Mode=TwoWay}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="Tilbud Afsendt:" />
            <TextBox Text="{Binding OfferSent}" />

            <TextBlock Text="Tilbud Godkendt:" />
            <TextBox Text="{Binding OfferApproved}" />

            <TextBlock Text="Pris (Anslået beregnes automatisk):" />
            <TextBox Text="{Binding Price}" />

            <TextBlock Text="Betalt:" />
            <TextBox Text="{Binding Paid}" />

            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                <Button Content="Tilbage" Command="{Binding CancelCommand}" Width="100"/>
                <Button Content="Opret" Command="{Binding CreateProjectCommand}" Margin="10,0,0,0" Width="100"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Window>
