<Page x:Class="CarlssonsWPF.Views.Kunde.KundeMainWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CarlssonsWPF.Views.Kunde"
      mc:Ignorable="d" 
      d:DesignHeight="720" d:DesignWidth="1280"
      Title="KundeMainWindow">

    <Grid Margin="10" ShowGridLines="True">
        <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Kunder" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,10,0,10" Grid.Row="0"/>

        <DataGrid Grid.Row="1"
          Name="CustomerDataGrid"
          Margin="0,10,0,10"
          VerticalAlignment="Stretch"
          ItemsSource="{Binding customers}"
          IsReadOnly="True"
          CanUserSortColumns="True"
          AutoGenerateColumns="False"
          MouseDoubleClick="CustomerDataGrid_MouseDoubleClick">
          
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="Navn" Binding="{Binding Name}" CanUserSort="True" />
                <DataGridTextColumn Header="Adresse" Binding="{Binding Address}" CanUserSort="True"/>
                <DataGridTextColumn Header="Postnummer" Binding="{Binding PostalCode}" CanUserSort="True"/>
                <DataGridTextColumn Header="By" Binding="{Binding City}" />
                <DataGridTextColumn Header="Telefon" Binding="{Binding PhoneNumber}" CanUserSort="True"/>
                <DataGridTextColumn Header="E-mail" Binding="{Binding Email}" CanUserSort="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="Opret" Width="150" Height="50" HorizontalAlignment="Right" Margin="10" Click="AddCustomerButton_Click" Grid.Column="1"/>
            <Button Content="Søg" Width="150" Height="50" HorizontalAlignment="left" Margin="10" Click="KundeSearch_Click" Grid.Column="2"/>

        </Grid>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="Start" HorizontalAlignment="Stretch" Click="HomescreenButton_Click" Grid.Column="1" Margin="10" MinWidth="150" MinHeight="50"/>


        </Grid>
    </Grid>
</Page>
